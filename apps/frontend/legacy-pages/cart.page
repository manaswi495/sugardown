<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>Cart — Sugar Down</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">

</head>
<body>
<nav>
  <a href="index.html" class="nl"><span>SUGAR DOWN</span><span>BY LUCKY HEART AYURVEDA</span></a>
  <ul class="nm">
    <li><a href="index.html">Home</a></li>
    <li><a href="products.html">Products</a></li>
    <li><a href="about.html">About</a></li>
    <li><a href="contact.html">Contact</a></li>
    <li><a href="track.html">Track Order</a></li>
  </ul>
  <div class="nav-actions">
    <a href="cart.html" class="nav-cart act" aria-label="Shopping cart">
      <svg class="nav-cart-ic" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
      <span class="cart-badge">0</span>
    </a>
    <a href="checkout.html" class="nc">Checkout</a>
  </div>
</nav>

<div class="wrap">
  <div class="pg-header">
    <div class="breadcrumb"><a href="index.html">Home</a> › <a href="products.html">Products</a> › Cart</div>
    <h1 class="pg-title">Your Cart</h1>
  </div>

  <div class="cart-layout">
    <div>
      <div class="cart-items" id="cart-items-container">
        <!-- Filled by JS -->
      </div>
      <div style="margin-top:16px;display:flex;justify-content:space-between;align-items:center;">
        <a href="products.html" style="font-size:13px;color:var(--gm);text-decoration:none;">← Continue Shopping</a>
        <button onclick="clearCart()" style="font-size:12px;color:var(--red);background:none;border:none;cursor:pointer;">Clear Cart</button>
      </div>
    </div>

    <div>
      <div class="summary">
        <h3>Order Summary</h3>
        <div class="sum-row"><span>Subtotal</span><span id="sum-sub">₹0</span></div>
        <div class="sum-row"><span>GST (5%)</span><span id="sum-gst">₹0</span></div>
        <div class="sum-row"><span>Discount (5%)</span><span id="sum-disc" style="color:var(--gm);">-₹0</span></div>
        <div class="sum-row"><span>Shipping</span><span style="color:var(--gm);">FREE 🎉</span></div>
        <div class="sum-divider"></div>
        <div class="sum-total"><span>Total</span><span id="sum-total">₹0</span></div>
        <div class="sum-total-sub" id="sum-perday"></div>
        <div class="coupon-row">
          <input class="coupon-input" id="coupon-in" placeholder="Have a coupon?" />
          <button class="coupon-btn" onclick="applyCoupon()">Apply</button>
        </div>
        <div class="coupon-msg" id="coupon-msg"></div>
        <a href="checkout.html" class="checkout-btn" id="checkout-btn">Proceed to Checkout →</a>
        <div class="sum-trust">
          <div class="st-item"><span>🔒</span><span>Secure & encrypted checkout</span></div>
          <div class="st-item"><span>🚚</span><span>Free shipping across India</span></div>
          <div class="st-item"><span>📦</span><span>Delivered in 3–5 working days</span></div>
        </div>
      </div>

      <div class="also-like" id="also-like" style="display:none;">
        <h4>You might also like</h4>
      </div>
    </div>
  </div>
</div>

<footer class="premium-footer">
  <style>
    .premium-footer {
      background: #1B4D2E;
      padding: 100px 0 60px;
      color: #fff !important;
      font-family: 'DM Sans', sans-serif;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      margin-top: 80px;
      position: relative;
      z-index: 10;
    }
    .footer-top {
      width: 100%;
      padding: 0 6%;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1.8fr 1fr 1.2fr;
      gap: 80px;
      margin-bottom: 80px;
    }
    .footer-logo {
      display: flex;
      flex-direction: column;
      margin-bottom: 28px;
    }
    .logo-main {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      font-weight: 900;
      letter-spacing: 2px;
      color: #fff;
    }
    .logo-sub {
      font-size: 11px;
      color: #C8972A;
      letter-spacing: 4px;
      text-transform: uppercase;
      margin-top: 4px;
      font-weight: 700;
    }
    .footer-tagline {
      font-size: 16px;
      color: rgba(255,255,255,0.8);
      line-height: 1.8;
      margin-bottom: 24px;
      max-width: 400px;
    }
    .footer-mission {
      font-size: 14px;
      color: rgba(255,255,255,0.5);
      line-height: 1.6;
      max-width: 400px;
    }
    .col-title {
      font-size: 14px;
      font-weight: 800;
      letter-spacing: 3px;
      margin-bottom: 32px;
      color: #fff;
      text-transform: uppercase;
      position: relative;
    }
    .col-title::after {
      content: '';
      position: absolute;
      bottom: -12px;
      left: 0;
      width: 30px;
      height: 2px;
      background: #C8972A;
    }
    .footer-link-list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 16px;
      padding: 0;
      margin: 0;
    }
    .footer-link-list a {
      color: rgba(255,255,255,0.7);
      text-decoration: none;
      font-size: 15px;
      transition: all 0.3s;
    }
    .footer-link-list a:hover {
      color: #C8972A;
      padding-left: 8px;
    }
    .support-text {
      font-size: 15px;
      color: rgba(255,255,255,0.6);
      margin-bottom: 24px;
      font-weight: 500;
    }
    .support-link {
      display: flex;
      align-items: center;
      gap: 12px;
      color: #fff;
      text-decoration: none;
      font-size: 15px;
      font-weight: 600;
      margin-bottom: 16px;
      transition: all 0.3s;
    }
    .support-link:hover {
      color: #C8972A;
    }
    .footer-quick-actions {
      margin-top: 32px;
      padding-top: 24px;
      border-top: 1px solid rgba(255,255,255,0.1);
    }
    .action-label {
      font-size: 11px;
      font-weight: 800;
      color: rgba(255,255,255,0.4);
      letter-spacing: 2px;
      display: block;
      margin-bottom: 16px;
    }
    .action-btns {
      display: flex;
      gap: 12px;
    }
    .action-btn {
      flex: 1;
      padding: 12px;
      border-radius: 8px;
      background: rgba(255,255,255,0.1);
      border: 1px solid rgba(255,255,255,0.1);
      color: #fff;
      text-decoration: none;
      font-size: 13px;
      font-weight: 700;
      text-align: center;
      transition: all 0.3s;
    }
    .action-btn:hover {
      background: #C8972A;
      color: #1B4D2E;
      border-color: #C8972A;
      transform: translateY(-2px);
    }
    .footer-divider {
      width: 100%;
      padding: 0 6%;
      margin: 0 auto 50px;
      height: 1px;
      background: rgba(255,255,255,0.1);
      background-clip: content-box;
    }
    .footer-bottom {
      width: 100%;
      padding: 0 6%;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 40px;
    }
    .footer-bottom-left {
      display: flex;
      flex-direction: column;
      gap: 20px;
      align-items: flex-start;
    }
    .footer-socials {
      display: flex;
      gap: 20px;
    }
    .social-icon {
      width: 44px;
      height: 44px;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s;
      text-decoration: none;
      color: #fff;
    }
    .social-icon:hover {
      background: #C8972A;
      border-color: #C8972A;
      color: #1B4D2E;
      transform: scale(1.1) rotate(5deg);
    }
    .footer-copyright {
      font-size: 12px;
      color: rgba(255,255,255,0.4);
      font-weight: 600;
      letter-spacing: 1px;
    }
    .footer-bottom-links {
      display: flex;
      gap: 40px;
    }
    .footer-bottom-links a {
      color: #fff;
      text-decoration: none;
      font-size: 12px;
      font-weight: 800;
      letter-spacing: 2px;
      transition: color 0.3s;
    }
    .footer-bottom-links a:hover {
      color: #C8972A;
    }
    @media (max-width: 900px) {
      .footer-top {
        grid-template-columns: 1fr;
        gap: 60px;
      }
      .footer-bottom {
        flex-direction: column;
        align-items: center;
        text-align: center;
      }
      .footer-bottom-left {
        align-items: center;
      }
      .footer-bottom-links {
        justify-content: center;
        flex-wrap: wrap;
        gap: 20px 30px;
      }
    }
  </style>
  <div class="footer-top">
    <div class="footer-col about">
      <div class="footer-logo">
        <span class="logo-main">SUGAR DOWN</span>
        <span class="logo-sub">BY LUCKY HEART</span>
      </div>
      <p class="footer-tagline">
        India's most focused Ayurvedic diabetes care system. Built for results, backed by support, and trusted by thousands.
      </p>
      <p class="footer-mission">
        Clear guidance, responsible care, and dependable follow-ups for your long-term health journey.
      </p>
    </div>
    <div class="footer-col links">
      <h4 class="col-title">LEGAL</h4>
      <ul class="footer-link-list">
        <li><a href="about.html">About Us</a></li>
        <li><a href="terms.html">Terms & Conditions</a></li>
        <li><a href="privacy.html">Privacy Policy</a></li>
        <li><a href="refund.html">Refund & Cancellation</a></li>
      </ul>
    </div>
    <div class="footer-col support">
      <h4 class="col-title">SUPPORT</h4>
      <p class="support-text">Need help? We're online 24/7</p>
      <a href="mailto:luckyheartayurveda@gmail.com" class="support-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
        luckyheartayurveda@gmail.com
      </a>
      <a href="tel:+918650777799" class="support-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.81 12.81 0 0 0 .62 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.62A2 2 0 0 1 22 16.92z"></path></svg>
        +91 8650777799
      </a>
      <div class="footer-quick-actions">
        <span class="action-label">QUICK ACTIONS</span>
        <div class="action-btns">
          <a href="tel:+918650777799" class="action-btn">Call</a>
          <a href="https://wa.me/918650777799" class="action-btn">WhatsApp</a>
        </div>
      </div>
    </div>
  </div>
  <div class="footer-divider"></div>
  <div class="footer-bottom">
    <div class="footer-bottom-left">
      <div class="footer-socials">
        <a href="https://www.instagram.com/_sugardown?igsh=MWN3cXIvNDI6anl6dg==" class="social-icon" title="Instagram" target="_blank">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
        </a>
        <a href="https://www.facebook.com/share/1D5GjEUUbF/" class="social-icon" title="Facebook" target="_blank">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path></svg>
        </a>
      </div>
      <div class="footer-copyright">
        © 2026 LUCKY HEART. ALL RIGHTS RESERVED.
      </div>
    </div>
    <div class="footer-bottom-links">
      <a href="products.html">PRODUCTS</a>
      <a href="contact.html">SUPPORT</a>
      <a href="#faq">FAQ</a>
      <a href="sitemap.xml">SITEMAP</a>
    </div>
  </div>
</footer>

<script src="shared.js"></script>
<script>
const THUMBS = {kwath:'t1',daycaps:'t2',sleepcaps:'t3',corekit:'t4',balkit:'t5'};
const COUPONS = {SUGAR10:{type:'pct',val:10,label:'10% off applied!'},LUCKY5:{type:'pct',val:5,label:'5% off applied!'},FIRST15:{type:'pct',val:15,label:'15% off — Welcome!'}};
let extraDiscount = 0;

function renderCart() {
  const cart = getCart();
  const container = document.getElementById('cart-items-container');
  if (cart.length === 0) {
    container.innerHTML = `<div class="cart-empty"><div class="cart-empty-icon">🛒</div><h3>Your cart is empty</h3><p>Add some products to get started.</p><a href="products.html">Browse Products</a></div>`;
    document.getElementById('checkout-btn').style.opacity='0.4';
    document.getElementById('checkout-btn').style.pointerEvents='none';
    updateSummary();
    return;
  }
  let html = `<div class="ci-header"><h3>Cart Items</h3><span class="ci-count">${cart.length} item${cart.length>1?'s':''}</span></div>`;
  cart.forEach(item => {
    const p = PRODUCTS[item.id];
    if (!p) return;
    const total = p.price * item.qty;
    html += `<div class="ci-item" id="ci-${item.id}">
      <div class="ci-thumb ${THUMBS[item.id]||'t4'}">${p.img}</div>
      <div class="ci-info">
        <div class="ci-name">${p.name}</div>
        <div class="ci-sub">${p.sub}</div>
        <span class="ci-tag">${p.tag}</span>
      </div>
      <div class="ci-right">
        <div class="ci-price">₹${total.toLocaleString()}</div>
        <div class="qty-ctrl">
          <button class="qty-btn" onclick="changeQty('${item.id}',-1)">−</button>
          <span class="qty-val" id="qty-${item.id}">${item.qty}</span>
          <button class="qty-btn" onclick="changeQty('${item.id}',1)">+</button>
        </div>
        <button class="ci-remove" onclick="removeItem('${item.id}')">Remove</button>
      </div>
    </div>`;
  });
  container.innerHTML = html;
  updateSummary();
  updateCartBadge();
}

function changeQty(id, delta) {
  const cart = getCart();
  const item = cart.find(i => i.id === id);
  if (!item) return;
  item.qty = Math.max(1, item.qty + delta);
  saveCart(cart);
  renderCart();
}

function removeItem(id) {
  let cart = getCart().filter(i => i.id !== id);
  saveCart(cart);
  renderCart();
}

function clearCart() {
  if (confirm('Clear all items?')) { saveCart([]); renderCart(); }
}

function updateSummary() {
  const cart = getCart();
  const sub = cart.reduce((s,i)=>{ const p=PRODUCTS[i.id]; return s+(p?p.price*i.qty:0); },0);
  
  // First add GST
  const gst = Math.round(sub * 0.05);
  const withGst = sub + gst;
  
  // Then calculate discount (on subtotal + gst)
  const disc5 = Math.round(withGst * 0.05);
  
  // Handle extra coupon discount (calculate on withGst if it's percentage, or just subtract if flat)
  // For simplicity and consistency, let's keep extraDiscount as it is or re-calculate it in applyCoupon
  
  const total = withGst - disc5 - extraDiscount;
  
  document.getElementById('sum-sub').textContent = '₹'+sub.toLocaleString();
  document.getElementById('sum-disc').textContent = '-₹'+(disc5+extraDiscount).toLocaleString();
  document.getElementById('sum-gst').textContent = '₹'+gst.toLocaleString();
  document.getElementById('sum-total').textContent = '₹'+total.toLocaleString();

  const days = Math.round(total/30);
  document.getElementById('sum-perday').textContent = sub>0?'Just ₹'+days+'/day':'';
  localStorage.setItem('sd_order_total', total);
}

function applyCoupon() {
  const code = document.getElementById('coupon-in').value.trim().toUpperCase();
  const msg = document.getElementById('coupon-msg');
  if (COUPONS[code]) {
    const c = COUPONS[code];
    const sub = getCart().reduce((s,i)=>{ const p=PRODUCTS[i.id]; return s+(p?p.price*i.qty:0); },0);
    const gst = Math.round(sub * 0.05);
    const withGst = sub + gst;
    extraDiscount = c.type==='pct' ? Math.round(withGst*c.val/100) : c.val;
    msg.className='coupon-msg ok'; msg.textContent='✓ '+c.label;
    updateSummary();
  } else {
    msg.className='coupon-msg err'; msg.textContent='✗ Invalid coupon code';
    extraDiscount=0; updateSummary();
  }
}

renderCart();
</script>
</body></html>

